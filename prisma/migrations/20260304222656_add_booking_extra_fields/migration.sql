ALTER TABLE "Booking"
  ADD COLUMN IF NOT EXISTS "customerEmail"    TEXT,
  ADD COLUMN IF NOT EXISTS "pickupLocation"   TEXT,
  ADD COLUMN IF NOT EXISTS "destination"      TEXT,
  ADD COLUMN IF NOT EXISTS "numberOfGuests"   INTEGER,
  ADD COLUMN IF NOT EXISTS "numberOfChildren" INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "numberOfPets"     INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "notes"            TEXT;
